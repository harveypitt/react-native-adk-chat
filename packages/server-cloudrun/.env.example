# Cloud Run ADK Proxy Configuration

# The base URL of your Cloud Run ADK deployment
CLOUD_RUN_URL=https://your-service-name.us-central1.run.app

# Default app name (can be overridden per request)
DEFAULT_APP_NAME=my_agent

# Port for the proxy server
PORT=3001

# Enable debug logging (optional)
DEBUG=false
